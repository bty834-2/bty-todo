# 待办事项应用 (bty-todo) - 规范文档

## 1. 项目概述

### 项目名称
bty-todo

### 项目类型
跨平台桌面端待办事项管理应用

### 核心功能
基于 Tauri 2 框架的待办事项应用，支持四种优先级分类（紧急/重要维度）、提醒时间设置、列表/四象限双视图展示、拖拽排序、明暗主题切换。

### 目标用户
需要管理日常任务、区分任务优先级并设置提醒的个人用户。

---

## 2. 功能规范

### 2.1 核心功能

#### 待办事项管理
- 创建待办（标题、描述、优先级、提醒时间）
- 编辑待办
- 删除待办
- 标记完成/未完成
- 拖拽调整顺序和优先级

#### 优先级分类
| 标识 | 名称 | 颜色 | 说明 |
|------|------|------|------|
| P0 | 紧急且重要 | 红色 #EF4444 | 立即处理 |
| P1 | 紧急不重要 | 橙色 #F97316 | 尽快处理 |
| P2 | 重要不紧急 | 蓝色 #3B82F6 | 计划处理 |
| P3 | 不重要不紧急 | 灰色 #6B7280 | 可延后 |

#### 视图模式
- **列表视图**：按添加顺序显示待办，底部有新增按钮
- **四象限视图**：按紧急/重要分为4个区域，每象限可独立添加

#### 筛选功能
- 全部：显示所有
- 未完成：只显示进行中
- 已完成：只显示已完成（添加按钮禁用）

#### 提醒功能
- 快捷选项：30分钟后、1小时后、1天后、2天后、3天后
- 自定义日期时间
- 到期发送 macOS 系统通知

#### 主题切换
- 明亮模式：浅灰蓝配色 (#F8FAFC 背景)
- 暗黑模式：macOS 风格深灰 (#1C1C1E 背景)
- 平滑过渡动画 (0.3s)

### 2.2 数据模型

```typescript
interface Todo {
  id: string;              // UUID
  title: string;           // 标题
  description: string;     // 描述
  priority: Priority;      // P0/P1/P2/P3
  reminderTime: string | null;  // ISO 格式
  completed: boolean;      // 是否完成
  createdAt: string;      // 创建时间
  updatedAt: string;       // 更新时间
  sortOrder: number;       // 排序序号
}

type Priority = 'p0' | 'p1' | 'p2' | 'p3';
```

### 2.3 数据存储
- 格式：JSON 文件
- 路径：
  - macOS: `~/Library/Application Support/com.mamba.bty-todo/todos.json`
  - Windows: `%APPDATA%\com.mamba.bty-todo\todos.json`

---

## 3. UI/UX 设计

### 3.1 布局

#### 头部
- 标题 "待办事项"
- 筛选标签（全部/未完成/已完成）
- 主题切换按钮
- 视图切换按钮（列表/四象限）

#### 列表视图
```
+----------------------------------+
| 待办 (3)                    [+] |
| ┌────────────────────────────┐ |
| │ ▌☐ 事项1      [P0]     × │ |
| │ ▌☐ 事项2      [P2]     × │ |
| └────────────────────────────┘ |
| 已完成 (1)                      |
| ┌────────────────────────────┐ |
| │ ▌✓ 事项3      [P1]     × │ |
| └────────────────────────────┘ |
|        [ + 添加待办 ]           |
+----------------------------------+
```

#### 四象限视图
```
+----------------------------------+
|       重要          不重要        |
|   ┌──────────┐  ┌──────────┐    |
| 紧│ 紧急且重要│  │紧急不重要 │    |
| 急│   P0    │  │   P1    │    |
|   │ [+添加]  │  │ [+添加]  │    |
|   └──────────┘  └──────────┘    |
|   ┌──────────┐  ┌──────────┐    |
| 不│重要不紧急│  │不重要不紧急│    |
| 紧│   P2    │  │   P3    │    |
|   │ [+添加]  │  │ [+添加]  │    |
|   └──────────┘  └──────────┘    |
+----------------------------------+
```

### 3.2 配色

#### 亮色主题
| 变量 | 颜色 | 用途 |
|------|------|------|
| bg-primary | #F8FAFC | 页面背景 |
| bg-secondary | #FFFFFF | 卡片背景 |
| bg-tertiary | #F1F5F9 | 按钮背景 |
| text-primary | #1E293B | 主文字 |
| text-secondary | #64748B | 次文字 |
| border-color | #E2E8F0 | 边框 |
| primary-color | #3B82F6 | 主色调 |

#### 暗色主题 (macOS 风格)
| 变量 | 颜色 | 用途 |
|------|------|------|
| bg-primary | #1C1C1E | 页面背景 |
| bg-secondary | #2C2C2E | 卡片背景 |
| bg-tertiary | #3A3A3C | 按钮背景 |
| text-primary | #F5F5F7 | 主文字 |
| text-secondary | #98989D | 次文字 |
| border-color | #38383A | 边框 |
| primary-color | #0A84FF | 主色调 (macOS蓝) |

### 3.3 组件样式

#### 待办卡片
- 左侧 3px 优先级颜色条
- 圆角 10px
- hover 时阴影加深
- 删除按钮默认隐藏，hover 时显示

#### 优先级标签
- 实心背景 + 白色文字
- 圆角 5px
- 字号 11px

#### 添加按钮
- 列表底部：虚线边框长方形，与卡片等宽
- 四象限内：虚线边框，宽度100%

---

## 4. 技术架构

### 4.1 前端
- React 18 + TypeScript
- Vite 构建工具
- @dnd-kit/core 拖拽库
- CSS 变量实现主题

### 4.2 后端
- Tauri 2 (Rust)
- serde 序列化
- tokio 异步运行时
- tauri-plugin-notification 通知
- tauri-plugin-fs 文件存储

### 4.3 项目结构
```
├── src/                    # 前端
│   ├── components/         # 组件
│   ├── types/             # 类型定义
│   ├── utils/             # 工具函数
│   ├── App.tsx            # 主组件
│   └── App.css            # 样式
├── src-tauri/             # 后端
│   ├── src/lib.rs         # 业务逻辑
│   └── tauri.conf.json    # 配置
└── README.md             # 说明
```

---

## 5. 验收标准

### 功能
- [x] 创建/编辑/删除待办
- [x] 四种优先级分类
- [x] 列表/四象限视图切换
- [x] 筛选全部/未完成/已完成
- [x] 拖拽排序和调整优先级
- [x] 快捷提醒设置
- [x] 系统通知提醒
- [x] 明亮/暗黑模式切换
- [x] 数据持久化存储
- [x] 视图偏好记忆

### 视觉
- [x] 优先级颜色鲜明
- [x] 扁平简洁设计
- [x] 平滑过渡动画
- [x] 适配 macOS/Windows
